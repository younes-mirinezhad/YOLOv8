# YOLOv8
1. Use Train directory to train your model on custom data  
	1. Make a copy of BaseFiles folder for your project  
	2. Put your dataset images and labels in dataset directory  
	3. Edir config.yaml (model_name, project, name, epochs, imgsz, batch, device, ...)  
	4. Edit dataset.yaml (path, names, ...)  
	5. Edit hyperparameters.yaml to add your augmentations
2. Use .... 
